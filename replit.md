# Project Overview

A minimal Python starter project.

## Structure

- `main.py` - Entry point, prints "Hello World"

## Setup

- Language: Python 3.12
- No dependencies
- Workflow: "Start application" runs `python main.py` as a console process
